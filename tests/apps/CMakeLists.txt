# ---------------------------------------------------------------------------
# tests/apps/ —— 进程集成测试 (镜像 apps/ 树)
#
# 每个子目录对应 apps/ 下某个进程的业务测试:
#   iot_agent/   → OtaManager 状态机, manifest 解析, SHA256 校验, TB 协议
#   mediad/      → ConfigStore 配置加载/校验/OSD 规范化
#
# 进程级测试 ≠ 库单测: 测的是 apps/<进程>/src/ 下业务代码, 不是 libs/ 下的纯函数。
# 这些代码通常依赖具体进程的上下文 (云平台协议、进程内状态机), 单测通过 fake/mock 验证。
# ---------------------------------------------------------------------------
add_subdirectory(iot_agent)
add_subdirectory(mediad)
