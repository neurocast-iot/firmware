# ---------------------------------------------------------------------------
# tests/test_support/ —— 跨测试共享工具
#
# 准入门槛: 至少被 2 个不同测试用到才放这里, 避免变成"杂物间"。
# 单测试自用的辅助函数 (如 mkstemp 包装) 留在那个测试文件里更清楚。
#
# 已收录:
#   - fake_http_server    libmicrohttpd 包装, 多个 libs/ota 测试在用
# ---------------------------------------------------------------------------
add_subdirectory(fake_http_server)
